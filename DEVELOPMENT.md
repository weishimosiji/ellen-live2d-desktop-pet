# 艾莲桌宠开发记录

最后更新：2026-08-26

## 项目目标

使用 Electron、Live2D 和轻量 Agent 制作艾莲桌宠。桌宠负责日常交互、语音输入和简单工具；复杂任务后续可以转交给独立学习 Agent。

## 当前运行方式

```bash
cd "/Users/wuyunxin/Desktop/zzz/艾莲桌面管理/desktop-preview"
npm start
```

每次修改后先执行：

```bash
npm run build
```

## 已完成功能

### Live2D

- 加载 Cubism 5.3 SDK 模型和多个 motion3 动作。
- Retina 分辨率适配，减少模型模糊。
- 鼠标视线跟随，可调节幅度。
- 自动眨眼，可独立启用和关闭。
- 动作结束后恢复眼睛开闭、眼睛微笑等初始参数。
- 动作、语音和组合任务使用严格串行队列。
- 动作配音不驱动代码口型，嘴形完全遵循 motion 文件。
- 普通交互语音通过音量驱动 `ParamMouthOpenY`。
- 当前模型嘴部参数方向：`1=闭嘴，0=完全张嘴`。

### 音频与角色交互

- `public/audio/motion_audio/`：与动作同名的动作配音。
- `public/audio/`：普通交互和 Agent 情绪语音。
- 支持眉眼语气：略微生气、很生气，音频结束后恢复。
- 启动时播放“为你服务”。
- 计算或文件工具执行后播放 `hihiboss`。
- 困难问题可由 Agent 选择播放 `mafan`。
- 最小化前播放 `seeyou`，播放完成后最小化。
- 关闭前播放 `byebye`，播放完成后关闭窗口。
- 鼠标移入人物身体播放 `touch`；点击身体播放 `no`。
- 鼠标交互只在播放器完全空闲时生效，忙碌时直接忽略，不进入等待队列。
- 鼠标命中使用 ArtMesh 加人体椭圆区域双重判断，默认排除透明区域和尾巴。
- 人体触发范围位于 `src/live2d/config.js` 的 `interactionRegions`。

### 本地语音识别

- 使用本地 whisper.cpp 进行语音转文字，不需要语音识别 API Key。
- 当前快捷键：应用处于前台时，按住 `Option（Alt）` 录音，松开后识别。
- 单次录音最长30秒。
- 识别结果进入语音修复流程。
- 清晰的普通聊天自动提交给 Agent。
- 逻辑不清晰、计算或文件操作会先显示原文和修复文本，等待用户确认。
- 修复器会结合最近对话处理同音字、错误分词、漏词和断句。

### 待办日历

- 提供月历视图，日期上显示未完成待办标记。
- 可以按日期添加待办和时间，并手动完成或删除。
- 数据保存在 Electron 用户数据目录的 `todos.json`，重启应用后保留。
- Agent 支持添加、查询、完成和删除待办；语音工具操作需要确认。
- 新增 `resolve_datetime` 工具，先把中文相对日期时间转换为 `YYYY-MM-DD` / `HH:mm`，再由 `add_todo` 写入。
- `add_todo` 只生成待办草稿，用户确认后页面才真正写入本地日历。

### 番茄钟与倒计时

- `resolve_duration` 将中文时长转换为秒，`start_timer` 在主进程启动倒计时。
- 支持纯倒计时和带事项倒计时，窗口最小化后仍继续运行。
- 到点显示系统通知、艾莲文字提醒，并尝试播放提示语音。
- 计时器持久化到用户数据目录，应用重启后继续恢复。

### 轻量 Agent

- DeepSeek 请求仅在 Electron 主进程执行。
- API Key 从项目根目录 `config.js` 的 `dsKey` 读取，不暴露给网页。
- `config.js` 已加入 `.gitignore`，禁止提交或分享真实密钥。
- 顶层提示词定义艾莲的性格、表达方式和工具决策规则。
- 默认保持人物沉浸感，不主动说“我是程序”或“没有实体”。
- 支持短期多轮上下文和清空上下文。
- 当前工具：
  - `resolve_datetime`：把中文日期时间表达转换为日历格式。
  - `add_todo` / `list_todos` / `complete_todo` / `delete_todo`：管理待办日历。
  - `classify_intent`：判断普通对话、计算和文件意图。
  - `calculate`：安全运算，不使用 `eval`。
  - `list_files`：列出安全工作区文件。
  - `read_file`：读取文本文件。
  - `write_file`：创建或覆盖文本文件。
  - `play_reaction_audio`：从白名单选择艾莲情绪语音。
  - `control_window`：只允许最小化或关闭当前桌宠窗口。
- 文件工具只能访问 `agent-workspace/`。
- 只允许 `.txt`、`.md`、`.json`、`.csv`，单文件不超过100KB。
- 当前不提供删除文件工具。

## 主要代码位置

| 功能 | 文件 |
| --- | --- |
| Electron 启动 | `electron-main.cjs` |
| 页面安全接口 | `electron/preload.cjs` |
| 页面初始化 | `src/app.js` |
| 模型初始化与命中检测 | `src/live2d/model.js` |
| 动作和音频队列 | `src/live2d/playback-queue.js` |
| 音量口型 | `src/live2d/audio-lipsync.js` |
| 鼠标触碰交互 | `src/live2d/pointer-interaction.js` |
| 模型参数配置 | `src/live2d/config.js` |
| 音频配置 | `src/config/audio-config.js` |
| 本地语音识别界面 | `src/voice/voice-recognition.js` |
| DeepSeek Agent | `electron/agent/deepseek-agent.cjs` |
| 艾莲系统提示词 | `electron/agent/system-prompt.cjs` |
| Agent 工具定义 | `electron/agent/tool-definitions.cjs` |
| Agent 工具执行 | `electron/agent/tool-executor.cjs` |
| Agent 对话界面 | `src/agent/agent-chat.js` |

## 后续功能

### 高优先级

- 全局按住说话：桌宠不在前台、最小化或用户正在使用其他软件时也能录音。
  - 推荐快捷键：`Command + Shift + Space`，避免全局占用单独的 Option。
  - 需要系统级 keydown/keyup 监听，Electron 自带全局快捷键无法可靠处理松开事件。
  - macOS 需要“系统设置 → 隐私与安全性 → 辅助功能”授权。
  - 主进程捕获按下/松开，再通过 IPC 通知页面开始和停止录音。
- 根据实际窗口测试继续校准 `interactionRegions`，使头、身体、手臂和腿部命中更贴合人物。
- 增加开发调试开关，在画布上临时显示鼠标命中区域和当前口型参数。

### 中优先级

- 为更多交互场景补充艾莲语音，并建立“场景—情绪—音频”配置表。
- 为 Agent 回复接入 TTS，生成普通对话语音；预录语气音继续用于高辨识度反应。
- TTS 播放期间沿用音量口型，动作配音继续使用 motion 口型。
- 增加托盘图标、开机启动、窗口置顶和透明桌宠模式。
- 将控制面板与正式桌宠窗口分离，桌宠模式隐藏调试控件。
- 增加 Agent 工具执行记录，显示调用了什么工具以及执行结果。

### 长期方案

- 将桌宠作为轻量入口，与艾莲学习 Agent 融合。
- 简单对话和桌面操作由桌宠本地 Agent 处理。
- 学习规划、资料整理和长任务交给复杂 Agent，并把进度反馈给桌宠。
- 增加长期记忆，但需要明确记忆范围、查看入口和删除机制。
- 打包、签名和发布 macOS 应用，处理麦克风与辅助功能权限说明。

## 当前需要注意

- 修改模型或 motion 后必须重新导出并确认程序引用的是最新文件。
- 动作最后一帧必须包含正确的恢复值，程序只能在动作中断时辅助重置。
- 普通语音口型参数方向与常见模型相反，不要把完全张嘴值改回 `1`。
- 启动语音依赖 Electron 的自动播放策略；浏览器模式可能仍要求首次用户交互。
- 真实 DeepSeek 请求会消耗 API 额度，构建测试不会验证 Key 或网络是否可用。
- 不要把 `config.js`、API Key 或个人音频提交到公开仓库。

## 更新记录

### 2026-08-26 UI 重构

- 按用户提供的艾莲都市工业风设计图重构桌宠界面。
- 人物改为视觉中心，回复显示在人物顶部气泡中。
- 麦克风、日历、番茄钟、历史记录和设置改为人物旁浮动按钮。
- 日历、番茄钟、历史记录和设置使用独立浮动面板。
- 待办写入确认改为应用内自定义确认卡片，不再使用系统确认框。
- 动作与交互语音收进左下角“动作调试”折叠面板。
- 对话历史在当前运行期间显示于历史记录面板。
- 从设计图裁切独立 PNG 图标，保存在 `public/assets/ui/icons/`。
- Electron 窗口改为 560×680 无边框透明桌宠，启动后固定在主屏幕右下角、始终置顶并从任务栏隐藏。
- 常驻窗口进一步缩小为 400×540，人物同步缩小并贴合窗口范围。
- 回复气泡默认隐藏，仅在艾莲回复或提醒时显示 7 秒；新增文字对话按钮，输入框按需展开。
- 增加独立 HTML 布局预览模式；浏览器中使用本地模拟接口，不依赖 Electron 即可调整界面。
- 废弃旧版 UI 切图引用，工具图标和所有面板改为纯 HTML/CSS 都市工业风样式。
- 从正式界面移除动作调试面板，现有动作索引、时长、配音和调用规则整理到 `ACTIONS.md`。
- 新增独立 `ui-theme.css`，统一重做番茄钟、历史记录、设置和待办日历浮层。
- 待办日历改为独立 380×500 副窗口，在桌宠左上方向打开；主窗口不再动态缩放。
- 将对话、语音确认、待办、番茄钟、历史和设置整合进统一功能副窗口；主窗口只保留人物与工具按钮。
- 功能副窗口打开在桌宠正上方，右边缘对齐并自动限制在当前显示器可见区域内。
- 改为直接拖动人物移动窗口；使用移动阈值区分拖动与点击，拖动期间工具栏保持显示。
- 触摸语音改为人物区域内静止停留 2 秒后触发；移动、按住或拖动都会取消计时。
- 持久化桌宠窗口位置，启动时恢复上次关闭前的右下角锚点。

### 2026-08-26

- 将录音改为前台按住 Option、松开识别。
- 将全局按住说话列入后续方案。
- 修正嘴部参数方向，放大普通语音口型。
- 收紧鼠标触碰区域，并让鼠标反馈在忙碌时直接忽略。
- 增加启动、工作工具、困难问题、最小化和关闭对应语音。
- 增加 Agent 情绪语音与窗口控制工具。
- 增加语音识别文本修复和确认流程。
